const {test}=require('node:test'),a=require('node:assert/strict'),fs=require('node:fs'),vm=require('node:vm'),XLSX=require('./vendor/xlsx.full.min.js'),M=require('./import-model.js');
test('downloadable Excel roundtrip retains groups and row numbers',()=>{const c={XLSX,ImportModel:M,presetNames:{book:'图书'},modal(){},sellerDetail(){}};vm.createContext(c);vm.runInContext(fs.readFileSync(__dirname+'/import-ui.js','utf8'),c);const w=vm.runInContext('importWorkbook(importSamples)',c);const bytes=XLSX.write(w,{type:'buffer',bookType:'xlsx'});const read=XLSX.read(bytes,{type:'buffer'});a.deepEqual(read.SheetNames,['商品导入','填写说明']);const rows=XLSX.utils.sheet_to_json(read.Sheets['商品导入'],{header:1,defval:'',blankrows:true,range:0});a.deepEqual(rows[0],M.headers);const result=M.validate(rows.slice(1));a.equal(result.valid.length,2);a.equal(result.rows[4].line,6);a.equal(result.valid[0].variants.length,2);});
