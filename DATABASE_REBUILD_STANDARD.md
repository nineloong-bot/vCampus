# vCampus 数据库重建总规范

> 版本：2026-09-16  
> 适用范围：`vcampus-database/schema`、`vcampus-database/seed`、批量数据生成器、Access 发布库  
> 数据库：Microsoft Access（UCanAccess），不得引入其他生产数据库

## 1. 目标与唯一事实来源

本次重建的目标是得到一套可重复生成、可审计、贴近真实高校业务、能够覆盖主要功能流程的 vCampus 初始数据库。数据库文件是生成产物，不是手工编辑的事实来源。

事实来源按以下优先级执行：

1. `vcampus-database/schema` 定义表结构、字段类型、索引和外键。
2. `vcampus-database/seed` 定义基础组织、账号、课程、培养方案及业务场景。
3. `vcampus-database/demo/full-test-data/tools` 只负责按规则生成规模化数据，不得另造一套业务含义。
4. `vcampus-distribution/data/vCampus.accdb` 只能由构建、校验通过后的临时数据库替换生成。

所有生成必须可重复。固定种子、固定规则、固定数据基准日和固定 ID 规则，不能依赖随机值或当前系统时间产生不可复现的差异。

## 2. 文本、命名与真实性要求

业务数据中禁止出现以下词及其大小写、中文变体或拼接形式：`test`、`测试`、`demo`、`演示`、`fake`、`sample`、`bulk`、`dummy`、`example`。禁止范围包括登录账号、姓名备注、身份证号、邮箱、书名、课程名、店铺名、商品名、申请理由、描述、班级名、组织名、主键和业务编号。

可以在代码注释、校验错误消息、文档标题和自动化测试方法名中使用“测试”语义，但不得写入数据库业务字段或随发布包提供的账号/数据清单。

姓名、手机号、邮箱、身份证号、地址、书籍、出版社、课程和组织名称使用彼此一致的中国高校场景。邮箱域名使用学校域名或真实可识别的公共邮箱域名；手机号为 11 位大陆手机号；身份证号必须通过校验位和出生日期校验，且同一证件号只能绑定一个学生。

## 3. 组织范围与人员规模

本次初始库只保留两个学院：

| 学院 | 专业 | 班级要求 |
| --- | --- | --- |
| 计算机科学与工程学院 | 计算机科学与技术、软件工程、人工智能 | 每个年级每个专业至少 1 个班，每班至少 5 名学生 |
| 数学学院 | 数学与应用数学、信息与计算科学 | 每个年级每个专业至少 1 个班，每班至少 5 名学生 |

学生年级覆盖 2024、2025、2026 三个入学年份。学生数量控制在可读、可维护的范围内，但要覆盖每个专业、每个班级、不同年级、不同学生状态和不同业务关系。推荐每个专业每个年级 1–2 个班，每班 4–12 人；严禁生成空班级。

需要生成一共120个学生，一卡通号范围如下：

- `213240001`–`213240040`
- `213250001`–`213250040`
- `213260001`–`213260040`

每个一卡通号全库唯一，必须一对一绑定 `tblUser` 与 `tblStudent`。一卡通号以及学号应与入学年份、学院/专业、班级保持一致，不能把一卡通号、学号、用户 ID、学生 ID 混为同一个概念。`CAMPUS_CARD_GLOBAL` 序列的当前值必须与已使用的一卡通号末段最大值一致，后续新增账号不能碰撞。如果对一卡通号和学号的规则不清楚，请一定要向我确认之后再开始工作。

## 4. 学生档案完整性

所有在读学生尽量填写 `tblStudent` 中可填写的个人、学籍和联系字段，不以大量 NULL 代替设计。至少必须有：姓名、姓名拼音、性别、出生日期、民族、政治面貌、婚姻状况、证件类型与号码、籍贯、出生地、生源地、户籍信息、健康状况、血型、身高、体重、特长、爱好、是否独生子女、手机号、邮箱、学院/专业/班级、入学日期、学生状态、校区、教育层次、培养方式、学制、预计毕业日期、辅导员信息。

字段之间必须合理：

- 本科生出生日期定死-24级入学06年1-8月出生；25级07年1-8月出生；26级08年1-8月出生，日期随机
- 入学日期为对应年份秋季学期日期；预计毕业日期与四年学制相符。
- 共青团员入团日期早于入学日期；党员日期只有在确有党员状态时填写。
- 学生当前学院、专业、班级、申请转专业前的来源信息必须一致。
- `enrolled`、`onCampus`、`studentStatus`、毕业日期不能互相矛盾。
- 学号、班级代码、专业代码和学院代码按统一编码规则生成，并通过关联校验。
- 身份证号校验要合理，不能生成无效的身份证号。

## 5. 课程与培养方案

培养方案只通过 `tblTrainingPlan`、`tblTrainingPlanCourse`、`tblTrainingPlanPrerequisite` 表表达，不得在其他模块复制一份课程培养数据。每个专业至少有一套有效培养方案，覆盖八个位置：

`(academicYearNo - 1) * 2 + seasonOrdinal`，其中学期只允许 `AUTUMN` 和 `SPRING`。

课程名称、课程代码、类别、学分和先修关系应贴近真实本科教学，例如高等数学、线性代数、大学物理、程序设计基础、数据结构、计算机组成原理、操作系统、数据库原理、软件工程、人工智能导论、数学分析、概率论与数理统计、抽象代数、数值分析等，可以进行网络搜索以更加贴切真实情况，覆盖必修选修以及跨学科三个部分。课程学分必须根据课程性质合理变化（如 1、2、3、4、5 学分均可按课程需要使用），不得把所有课程设为同一学分。

每套培养方案必须能解释：公共基础课、学科基础课、专业核心课、专业选修课、实践/实验课和毕业环节；课程不能安排在不存在的学期；先修课程不能形成环；课程总学分应落在合理本科范围，并能被选课和成绩模块读取。

选课运行数据遵循以下规则：

- 每门可选课程至少配置 2 个教学班；教学班必须完整填写任课教师、开课学期、上课周次、星期、节次、教室、容量、已选人数、考核方式、课程状态等字段，不能只有课程名称和编号。
- 初始库清空所有学生选课、退课和选退历史记录；学生初始不绑定任何待选课程，选课过程留给人工操作。
- 只开放一个选课学期，其他学期不得处于可选状态；培养方案仍然保留八学期课程规划。
- 保留课程容量、先修课和专业范围等选课约束，用于人工验证选课成功、满班、重复选课和先修课不足等流程。

## 6. 必须预置的业务场景

初始数据库必须至少包含以下可直接操作的场景：

1. 数学学院学生向计算机科学与工程学院申请转专业 5 人，来源学院、来源专业、来源班级、学生当前档案一致，目标专业为计算机科学与技术或软件工程/人工智能之一。
2. 上述 5 人全部都是已提交状态；申请批次时间必须与申请状态一致。
3. 图书馆至少有 1–3 条当前逾期借阅，逾期天数、罚款金额、借阅政策三者可重算一致；同时有正常借阅、已归还、续借、预约和不同图书副本状态。
4. 课程模块至少有可选课程、已选课程、课程容量边界、先修课和成绩记录；成绩不能出现在未选课程或不属于学生培养方案的课程上；注意：24级的学生应该有前四个学期培养方案中所有必修课的成绩记录，25级必须有前两个学期的，26级没有，选修课成绩可以适当加入24和25级学生成绩记录中。
5. 学籍模块至少覆盖资料完整、资料待审核、正常在读和已退学/休学等状态，但所有状态转换符合业务约束。
6. 用户模块必须预置以下 8 个管理员账号，账号名短、易记、不得使用历史演示账号：

   | 数量 | 登录号 | 角色/范围 |
   | ---: | --- | --- |
   | 1 | `ADMIN` | 超级管理员，负责平台级管理 |
   | 1 | `STUDENT` | 学籍子管理员 |
   | 1 | `COURSE` | 选课子管理员 |
   | 1 | `LIBRARY` | 图书子管理员 |
   | 1 | `SHOP` | 商城子管理员 |
   | 1 | `USER` | 用户子管理员 |
   | 1 | `CSADMIN` | 计算机科学与工程学院管理员 |
   | 1 | `MATHADMIN` | 数学学院管理员 |

   以上账号分别使用 `SUPER_ADMIN`、`STUDENT_ADMIN`、`COURSE_ADMIN`、`LIBRARY_ADMIN`、`SHOP_ADMIN`、`USER_ADMIN` 和两个带学院绑定的 `COLLEGE_ADMIN` 角色。学院管理员只能绑定对应学院；不得再保留第三个学院或无组织归属的学院管理员。所有管理员账号初始密码均为 `123456`，密码必须按既有哈希规则写入，账号名和密码说明必须出现在同一版本的账号清单中。
7. 所有学生账号的初始密码均为 `123456`，`tblUser.mustChangePassword` 必须为 `TRUE`，首次登录必须进入改密码流程；管理员和教师账号是否要求首次改密由账号矩阵明确设置，但不得影响学生强制改密。账号角色、组织权限、登录号和关联档案一致。
8. 如保留商城数据，商品、库存、购物车、订单、支付和店铺状态必须相互一致；不需要商城样本时应明确关闭该模块 seed，不保留残缺孤立记录。

图书逾期账号必须明确输出：账号清单中列出每个逾期借阅人的一卡通号、姓名、图书题名、条码、应还日期、逾期天数、应收罚款和当前账户余额。至少准备 2 名不同学生作为逾期罚款用户，分别覆盖轻度和较长逾期；逾期用户必须能通过学生账号登录并在图书与钱包模块中查到对应记录。

商城初始数据必须满足以下覆盖矩阵：

| 数据 | 数量及覆盖要求 |
| --- | --- |
| 已开店店铺 | 6 家，其中 5 家正常营业、1 家暂停营业 |
| 开店申请 | 至少 6 条已通过且对应现有店铺，另有 1 条待审核、1 条被驳回，总数至少 8 条 |
| 商品 | 72 件，每家 12 件；覆盖正常在售、草稿、下架和限制商品 |
| 多规格商品 | 至少 12 件，每件 2–3 个规格，默认规格有效 |
| 购物车 | 两名主要买家分别准备 6 行和 2 行商品；至少一名其他用户保持空购物车 |
| 订单 | 20 个店铺订单；订单组数量由实际跨店结算分组决定 |
| 专项资质 | 至少 5 条，覆盖有效通过、待审核、驳回、已到期、待审核续期各 1 条 |
| 举报及恢复申请 | 至少 5 条，覆盖待处理和已处理举报、店铺恢复、商品整改 |
| 处置与审核历史 | 与上述申请和处置一一对应，保留完整审计记录，不能只填当前状态 |
| 共享钱包 | 两名主要买家、六名店主及图书罚款用户拥有需要的账户和完整资金记录 |

## 7. 数据校验清单

构建临时 Access 数据库后，必须自动执行以下检查，任一失败不得替换发布库：

- 所有表存在，字段类型与 schema 一致，所有外键、唯一索引通过。
- 全库业务文本扫描通过禁词检查。
- 学院、专业、班级、学生、用户和一卡通一一对应且无孤儿记录。
- 每个配置专业/年级/班级至少有一名学生；没有空班级。
- 学号、一卡通号、登录号、身份证号、ISBN、条码均唯一且格式合法。
- 学生年龄、出生日期、入学日期、预计毕业日期、政治身份和党员/团员日期相互合理。
- 培养方案八学期完整，课程学分与先修关系有效，总学分在专业合理范围内。
- 每门可选课程至少有 2 个教学班，教学班关键字段完整；初始选课、退课及选退历史记录数量均为 0，且只有一个学期开放选课。
- 转专业 5 人的来源与目标组织、批次、状态、评分和申请时间一致。
- 借阅记录的副本状态、贷款状态、归还时间、到期时间和罚款金额一致；至少存在 1–3 条逾期罚款，并能从版本化账号清单明确定位逾期用户。
- 商城满足 6 家店铺、8 条开店申请、72 件商品、12 件多规格商品、8 行购物车、20 个店铺订单、5 条专项资质、5 条举报/恢复申请及相应历史记录和钱包流水的数量与状态要求。
- 账号初始密码只通过既有密码哈希规则生成，不在数据库中保存明文密码。
- 管理员账号恰好为 8 个，登录号为 `admin`、`student`、`course`、`library`、`shop`、`user`、`cs`、`math`；角色和学院绑定无误。
- 8 个管理员的初始口令均为 `123456`；120 个学生的初始口令均为 `123456` 且 `mustChangePassword=TRUE`，并能实际触发首次登录改密。
- 生成 SQL、数量快照、账号说明和最终 Access 文件使用同一数据版本标识。

## 8. 重建流程与安全边界

重建严格按以下顺序执行：

1. 停止使用中的服务并确认数据库文件未被锁定。
2. 将现有 `vCampus.accdb` 复制为带日期的备份，不删除备份。
3. 从 schema 创建临时 Access 文件。
4. 按基础字典、组织与账号、学生档案、课程与培养方案、图书与业务场景的顺序导入 seed。
5. 运行完整校验和数量快照；失败时只修改源 SQL/生成器，禁止手工修补 `.accdb`。
6. 在副本上执行必要的读写冒烟流程。
7. 校验、冒烟和 `mvn test` 均通过后，才用生成库替换 `vcampus-distribution/data/vCampus.accdb`。
8. 记录数据版本、生成时间、表数量、校验结果和备份路径。

任何重建命令必须明确目标文件路径，禁止对仓库根目录或不确定路径执行递归删除。重建过程中不得提交日志、锁文件、临时数据库、密码、IDE 文件或 Maven `target` 目录。

## 9. 版本与变更管理

每次数据变更必须同时更新：

- 本规范中受影响的数量、编码或场景要求；
- schema/seed/生成器及对应校验；
- 数量快照、账号清单和运行说明；
- 发布 Access 数据库及其数据版本标识。

提交前必须查看精确 diff，确认没有旧的 `test`、`测试`、`demo`、`演示`、`fake`、`sample`、`bulk`、`dummy`、`example` 业务数据回流。
