# Shop Manual Test 017–021 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` task-by-task in this session. Subagents are prohibited.

**Goal:** 修复购物车移除与部分结算、申请刷新与模态填写、店主商品分列及草稿流转，并将 Shop 用户可见错误统一为中文。

**Architecture:** 复用现有 Shop 服务端能力和稳定错误码。客户端以明确的选择状态、携带结算范围的内部路由、模态申请边界和双列表商品视图实现交互；服务端继续最终校验结算范围和商品状态转换。

**Tech Stack:** Java 21、Swing、Maven Reactor、JUnit 5、AssertJ、Mockito、Access/UCanAccess。

**Spec:** `docs/superpowers/specs/2026-09-01-vcampus-shop-manual-test-findings.md` 的 `SHOP-TEST-017` 至 `SHOP-TEST-021`。

## Global Constraints

- 只在 `E:\summer-school\vCampus\.worktrees\shop-auth-demo` 的 `SHOP` 分支工作。
- 仅修改 `common/shop`、`server/shop`、`client/shop`、Shop Demo/测试和说明文档。
- 不修改 Foundation、User、Socket、Router、事务框架或公共网络接口实现；不升级数据库；端口保持 `8888`。
- 保留且不提交或删除 `logs/` 与 `.superpowers/brainstorm/`。
- 每项严格执行 RED → 最小实现 → GREEN → 回归；没有再次授权不得提交或推送。
- 购物车初始全部不选；申请采用模态弹窗；草稿归入下架列且不能直接上架；界面错误使用中文但日志保留错误码。

## File Map

- New `client/shop/ui/ShopUiErrorsTest.java`: 错误码中文覆盖测试。
- New `client/shop/ui/seller/SellerApplicationDialogPort.java`: 可测试的申请弹窗边界。
- New `client/shop/ui/seller/SwingSellerApplicationDialog.java`: 模态申请填写与关闭保护。
- New `client/shop/ui/seller/SwingSellerApplicationDialogTest.java`: 弹窗状态机测试。
- Modify `ShopUiErrors`, all Shop failure renderers, `CartItemCard`, `CartPanel`, `CheckoutPanel`, `ShopRoute`, `ShopNavigationState`, `ShopPageCoordinator`, `SellerApplicationPanel`, `ApplicationReviewPanel`, `ProductManagementPanel`, `ProductService` and their existing tests.

---

### Task 1: Complete Chinese error mapping (SHOP-TEST-021)

**Files:**
- Create: `vcampus-client/src/test/java/edu/seu/vcampus/client/shop/ui/ShopUiErrorsTest.java`
- Modify: `vcampus-client/src/main/java/edu/seu/vcampus/client/shop/ui/ShopUiErrors.java`
- Modify: every production file returned by `rg -l "ShopUiErrors\.code" vcampus-client/src/main/java/edu/seu/vcampus/client/shop`

**Interfaces:** preserve `code(Throwable)` and `sessionExpired(String)`; add `message(Throwable)` and `message(String)`.

- [ ] Write RED tests enumerating every `ShopErrorCode`, common auth/validation/internal symbols, timeout/connection failures, and unknown fallback.

```java
@ParameterizedTest @EnumSource(ShopErrorCode.class)
void everyShopCodeHasChineseText(ShopErrorCode code) {
    assertThat(ShopUiErrors.message(code.name()))
            .doesNotContain(code.name()).matches(".*[\\u4e00-\\u9fff].*");
}
@Test void unknownKeepsCodeButShowsFallback() {
    Throwable error = new RuntimeException("UNKNOWN_CODE");
    assertThat(ShopUiErrors.code(error)).isEqualTo("UNKNOWN_CODE");
    assertThat(ShopUiErrors.message(error)).isEqualTo("操作失败，请稍后重试");
}
```

- [ ] Run RED:

```powershell
mvn -pl vcampus-client -am '-Dtest=ShopUiErrorsTest' '-Dsurefire.failIfNoSpecifiedTests=false' test
```

- [ ] Implement an immutable exhaustive map. Include action-oriented Chinese for all enum values plus `AUTH_SESSION_EXPIRED`, `COMMON_VALIDATION_FAILED`, `COMMON_INTERNAL_ERROR`, timeout and connection failures. Do not derive user text from exception messages.
- [ ] Change every UI path to retain `code` for session/log decisions and render `ShopUiErrors.message(code)`.
- [ ] Run GREEN and raw-code scan:

```powershell
mvn -pl vcampus-client -am '-Dtest=ShopUiErrorsTest,ShopUiTest,PurchasePanelsTest' '-Dsurefire.failIfNoSpecifiedTests=false' test
rg -n "setText\(code\)|showError\(code\)|stateView\([^\n]*code" vcampus-client/src/main/java/edu/seu/vcampus/client/shop
```

Expected: tests pass and no user-visible raw-code call remains.

---

### Task 2: Expose safe cart removal (SHOP-TEST-017)

**Files:** modify `CartItemCard.java`, `CartPanel.java`; test `PurchasePanelsTest.java`.

**Interfaces:** preserve `ShopClientPort.removeCartItem(String)`; visible action is `cart.remove-<id>` with text `移除`; use an injected confirmation boundary.

- [ ] Write RED tests that lay out each card at supported normal width, assert “移除” intersects visible bounds, cancel sends no request, and confirm removes only the target while authoritative count/total refresh.
- [ ] Run RED with `PurchasePanelsTest`.
- [ ] Re-layout the card so product details and quantity do not hide the bottom action row. Confirmation text is “确定移除此商品？”. Pending removal disables only the initiating action and continues using the existing serialized write queue.
- [ ] Run GREEN:

```powershell
mvn -pl vcampus-client -am '-Dtest=PurchasePanelsTest,ShopUiTest' '-Dsurefire.failIfNoSpecifiedTests=false' test
```

---

### Task 3: Implement partial-cart checkout (SHOP-TEST-018)

**Files:** modify `ShopRoute.java`, `ShopNavigationState.java`, `CartItemCard.java`, `CartPanel.java`, `CheckoutPanel.java`, `ShopPageCoordinator.java`; test `PurchasePanelsTest`, `ShopNavigationStateTest`, `ShopUiTest`, `CheckoutServiceTest`, and Shop E2E tests.

**Interfaces:** internal route becomes `record Checkout(Set<String> cartItemIds)` with `Set.copyOf`; public `CheckoutCommand` stays unchanged and contains only selected items.

- [ ] Write RED cart tests: initial selection empty; each row has `cart.select-<id>`; full-select and clear-select work; selected count/total update; checkout is disabled at zero selection and enabled otherwise.

```java
assertThat(panel.selectedItemIds()).isEmpty();
click("cart.select-item-2");
assertThat(panel.selectedItemIds()).containsExactly("item-2");
assertThat(component("cart.selected-total")).hasText("已选总计：¥5.00");
```

- [ ] Implement selection keyed by stable `cartItemId`, defaulting to empty. Preserve valid selections across cart refresh and discard IDs no longer present. When empty, disable “去结算” and show “请至少选择一件商品”；选中商品后清除该提示。Open `new ShopRoute.Checkout(Set.copyOf(selectedIds))`.
- [ ] Write RED checkout tests proving the confirmation page displays and submits only route-selected rows; unselected rows remain after successful checkout. If any selected ID disappeared, show “所选商品已变化，请返回购物车重新选择” and never fall back to all items.
- [ ] Implement selected filtering and selected total inside `CheckoutPanel`; retain authoritative server price/stock validation.
- [ ] Add/retain server test proving a subset `CheckoutCommand` deletes only submitted cart rows.
- [ ] Run GREEN:

```powershell
mvn -pl vcampus-server -am '-Dtest=CheckoutServiceTest' '-Dsurefire.failIfNoSpecifiedTests=false' test
mvn -pl vcampus-client -am '-Dtest=PurchasePanelsTest,ShopNavigationStateTest,ShopUiTest,ShopSellerAdminEndToEndTest' '-Dsurefire.failIfNoSpecifiedTests=false' test
```

---

### Task 4: Convert seller application to a modal workflow (SHOP-TEST-019)

**Files:** create `SellerApplicationDialogPort.java`, `SwingSellerApplicationDialog.java`, `SwingSellerApplicationDialogTest.java`; modify `SellerApplicationPanel.java` and `SellerApplicationPanelTest.java`.

**Interfaces:** `SellerApplicationDialogPort.open(Component, Optional<SellerApplicationView>, Runnable changed)`; summary page owns `seller.application.refresh`; dialog owns form, `保存草稿`, `直接提交`, and close prompt.

- [ ] Write RED summary tests: the page shows status/review reason, “刷新”, and “填写申请/修改申请”; editable fields are absent until the dialog opens; refresh issues exactly one load and is disabled in flight.
- [ ] Move editable form into the modal boundary. Because it is application-modal, the summary page and refresh cannot be clicked while open.
- [ ] Write RED dialog tests for:
  - valid form + 保存草稿 → only save, close after success;
  - valid form + 直接提交 → save then submit using returned ID/version;
  - dirty close → 保存草稿 / 不保存 / 取消;
  - clean close → no prompt;
  - failed mutation → dialog stays open, Chinese error shown, buttons restored;
  - PENDING/APPROVED read-only; REJECTED editable with review reason.
- [ ] Implement by reusing `LimitedTextInput` and `SellerApplicationLimits`; disable close during mutation; call `changed` only after successful mutation so the summary reloads.
- [ ] Run GREEN:

```powershell
mvn -pl vcampus-client -am '-Dtest=SellerApplicationPanelTest,SwingSellerApplicationDialogTest,ShopUiTest' '-Dsurefire.failIfNoSpecifiedTests=false' test
```

---

### Task 5: Add applicant/admin refresh controls (SHOP-TEST-019)

**Files:** modify and test `SellerApplicationPanel.java` and `ApplicationReviewPanel.java`.

- [ ] Write RED tests that applicant refresh rejects stale results; admin refresh loads PENDING and PROCESSED once each, stays disabled until both finish, preserves selected tab, and restores a selection by `applicationId` only when still present.
- [ ] Implement using existing `LatestRequest` guards. One admin refresh generation tracks both completions. A failure in either list uses the Chinese mapping and cannot allow an older response to overwrite newer data. Do not add polling.
- [ ] Run GREEN:

```powershell
mvn -pl vcampus-client -am '-Dtest=SellerApplicationPanelTest,ApplicationReviewPanelTest' '-Dsurefire.failIfNoSpecifiedTests=false' test
```

---

### Task 6: Enforce seller product transitions (SHOP-TEST-020)

**Files:** modify `ProductService.java`; test `ProductServiceTest.java`.

**Interfaces:** reuse `ChangeProductStatusCommand`, `SHOP_STATUS_INVALID`, and `SHOP_SKU_UNAVAILABLE`. Allowed transitions are only `DRAFT -> INACTIVE`, `INACTIVE -> ACTIVE`, `ACTIVE -> INACTIVE`.

- [ ] Write RED parameterized transition tests. Explicitly prove `DRAFT -> ACTIVE` and self/other transitions fail with `SHOP_STATUS_INVALID`; `DRAFT -> INACTIVE` requires at least one enabled variety; `INACTIVE -> ACTIVE` retains the enabled-variety-with-positive-available-stock rule.
- [ ] Run RED with `ProductServiceTest`.
- [ ] Add one transition validator:

```java
boolean allowed = from == DRAFT && to == INACTIVE
        || from == INACTIVE && to == ACTIVE
        || from == ACTIVE && to == INACTIVE;
if (!allowed) throw error(SHOP_STATUS_INVALID,
        "Invalid seller product status transition");
```

- [ ] Run GREEN regression:

```powershell
mvn -pl vcampus-server -am '-Dtest=ProductServiceTest,AdminProductServiceTest,CheckoutServiceTest' '-Dsurefire.failIfNoSpecifiedTests=false' test
```

---

### Task 7: Split seller products into two columns (SHOP-TEST-020)

**Files:** modify `ProductManagementPanel.java`; test `ProductManagementPanelTest.java`.

**Interfaces:** tables `seller.products.active-table` and `seller.products.inactive-table`; action `seller.products.status-action`; labels `已上架`, `已下架`, `草稿`.

- [ ] Write RED partition/layout tests: ACTIVE only left; INACTIVE and DRAFT right; draft displays “草稿”; selection in one table clears the other; both columns remain readable at normal window size.
- [ ] Write RED action tests:
  - ACTIVE → button “下架”, command to INACTIVE;
  - INACTIVE → button “上架”, command to ACTIVE;
  - DRAFT → button “完成商品编辑”, command to INACTIVE;
  - no selection → modify/status actions disabled.
- [ ] Implement a non-draggable `GridLayout(1, 2, 12, 0)` of titled panels. Query once and partition locally. Preserve the stale-detail request guard; after transitions reload and restore by stable product ID when possible.
- [ ] Run GREEN:

```powershell
mvn -pl vcampus-client -am '-Dtest=ProductManagementPanelTest,SwingProductEditorDialogsTest,SellerWorkspacePanelTest' '-Dsurefire.failIfNoSpecifiedTests=false' test
```

---

### Task 8: Full regression, evidence, and manual acceptance

**Files:** update the findings document and only necessary Shop Demo fixtures/tests.

- [ ] Run focused server/client suites for Tasks 1–7.
- [ ] Run full verification:

```powershell
mvn verify
git diff --check
git status --short
git diff --name-only
```

- [ ] Require `BUILD SUCCESS`, zero failures/errors, only authorized files, and protected untracked directories unchanged.
- [ ] Append exact RED/GREEN/full-suite evidence to `SHOP-TEST-017` through `021`, preserving original observations.
- [ ] Start with existing scripts and manually accept: visible confirmed removal; default-empty partial selection and retained unselected cart rows; modal application save/submit/close paths and both refresh buttons; two-column product state movement and draft completion; representative buyer/seller/admin errors all Chinese while logs retain codes.
- [ ] Stop and report evidence. Commit/push only after a new explicit authorization; stage exact files and exclude `logs/` plus `.superpowers/brainstorm/`.
