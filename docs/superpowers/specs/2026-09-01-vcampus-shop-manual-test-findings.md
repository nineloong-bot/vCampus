# vCampus Shop 手工测试问题汇总

**记录日期：** 2026-09-01
**测试对象：** `SHOP` 分支最终四角色 Demo
**文档性质：** 手工测试问题记录与分类，不作为具体整改设计或实施方案
**当前范围：** `SHOP-TEST-001` 至 `SHOP-TEST-016`，共 16 组、21 个具体问题

## 1. 测试背景与约束

本轮测试由用户使用最终 Demo 进行人工操作，覆盖商城首页、搜索、购物车、确认订单、开店申请、管理员工作区和卖家工作区。本文保存已经确认的问题现象、复现路径、用户期望与验收关注点，供后续统一策划整改方案。

本阶段只收集和整理问题，不在本文中确定具体技术实现。商品图片继续使用简单占位，不因本轮界面问题引入图片资源或数据库升级。

## 2. 问题总览

| 编号 | 问题摘要 | 分类 | 严重程度 |
| --- | --- | --- | --- |
| `SHOP-TEST-001` | 首次进入商城时页面异常偏长并出现大面积空白 | 页面布局 | 高 |
| `SHOP-TEST-002A` | 完整填写开店申请后仍被强制要求先保存草稿 | 开店申请 | 高 |
| `SHOP-TEST-002B` | 未保存申请内容时离开页面没有确认提示 | 数据保护 / 导航 | 高 |
| `SHOP-TEST-003.1` | 商品卡底部按钮重复展示商品信息 | 商品展示 | 中高 |
| `SHOP-TEST-003.2` | 首页与搜索结果没有区分店铺信息展示策略 | 商品展示 | 中 |
| `SHOP-TEST-004` | 管理员审核列表未区分未处理和已处理申请 | 管理后台 | 中高 |
| `SHOP-TEST-005` | 店长需要填写 SKU ID，卖家工作区过于技术化 | 卖家工作区 | 高 |
| `SHOP-TEST-006` | “筛选”和“应用筛选”同时出现，语义混淆 | 搜索交互 | 中 |
| `SHOP-TEST-007.1` | 购物车和确认订单缺少小计与总价 | 购物流程 | 严重 |
| `SHOP-TEST-007.2` | 购物车和确认订单横向排列商品 | 购物流程 / 布局 | 高 |
| `SHOP-TEST-007.3` | 购物车商品描述区域不能进入商品详情 | 购物流程 / 导航 | 中 |
| `SHOP-TEST-008` | 我的与购物车反复切换会污染返回历史 | 导航模型 | 高 |
| `SHOP-TEST-009` | 旧服务端占用 8888 时启动诊断信息不足 | Demo 启动 | 已解决 / 可选改进 |
| `SHOP-TEST-010` | 卖家商品管理页缩小后商品列表被严重压缩 | 卖家工作区 / 布局 | 高 |
| `SHOP-TEST-011` | 长浏览链受串行历史长度限制，最终无法返回首页 | 导航模型 | 高 |
| `SHOP-TEST-012` | 开店申请缺少字数限制，管理员无法查看超长联系方式和经营计划 | 开店申请 / 管理后台 | 高 |
| `SHOP-TEST-013` | 店主创建商品弹窗尺寸固定，窗口无法拉伸 | 卖家工作区 / 弹窗布局 | 高 |
| `SHOP-TEST-014.1` | 用户界面使用技术术语“SKU”，应统一改为“商品种类” | 全局文案 / 卖家工作区 | 中高 |
| `SHOP-TEST-014.2` | “更新所选商品”按钮文案不符合用户操作语义 | 卖家工作区 / 文案 | 中 |
| `SHOP-TEST-015` | 管理员商品管理页缩小后左侧店铺和商品表格被严重压缩 | 管理后台 / 布局 | 高 |
| `SHOP-TEST-016` | 商品详情页“店铺”按钮的动作含义不够明确 | 商品详情 / 文案 | 低 |

## 3. 按领域归类

### 3.1 页面布局与商品展示

#### SHOP-TEST-001：首次进入商城时页面高度异常

**影响范围：** 所有账号首次登录后进入商城首页。
**严重程度：** 高。

**现象：**

- 第一次进入商城首页时，商品内容区域异常偏长。
- 页面下半部分存在大量没有实际内容的空白。
- 垂直滚动范围明显超过当前商品内容所需高度。
- 点击“下一页”，再点击“上一页”回到第一页后，布局恢复正常。

**复现步骤：**

1. 启动客户端并登录任意固定账号。
2. 第一次进入商城首页。
3. 观察商品区域高度、页面底部空白和垂直滚动条。
4. 点击“下一页”。
5. 点击“上一页”返回第一页。
6. 对比首次进入与翻页后的页面高度。

**期望结果：**

- 第一次加载完成后立即按照窗口宽度和商品数量计算正确的网格高度。
- 页面底部不出现无意义的大面积空白。
- 首次加载与翻页返回后的页面布局一致。

**验收关注点：**

- 所有账号首次进入均正常。
- 不同窗口尺寸下均不会出现异常滚动范围。
- 异步商品加载完成后，布局和滚动容器能够正确刷新。

**人工复测结果（2026-09-01，最终便携 Demo）：** 未通过。用户补充的连续截图证明首次进入第一页时视口确实从顶部开始，问题不是初始滚动位置偏移。第一页的滚动内容总高度被异常放大：商品列表结束后仍有大段空白，分页按钮被推到异常内容区域的最底部。进入第二页后内容高度和分页位置恢复正常。`SHOP-TEST-001` 维持“未解决”状态。

**五张对比截图所证明的现象：**

1. 登录成功后第一页从搜索栏和分类栏正常开始，但右侧滚动条滑块异常短，说明滚动模型中的总内容高度远大于实际可见商品所需高度。
2. 滚动到第一页最后一批商品时仍看不到分页按钮，并且滚动条尚未到达中部。
3. 继续滚到底部后，中间出现大面积无商品内容的空白，分页按钮位于异常长页面的最底端。
4. 点击下一页后，第二页顶部布局正常，滚动条滑块明显变长，说明总内容高度已经按当前商品行数重新计算。
5. 第二页底部紧接最后一行商品显示分页按钮，不再存在第一页那种超长空白。

**此前采取的方案：**

- 引入 `WrappingGridLayout`，按照可用宽度计算列数，并按照商品数量、列数和卡片首选高度计算网格首选高度。
- 将商品卡固定为约 `200` 像素宽，在页面变宽时增加列数。
- 商品图片异步完成并重新渲染卡片后，对卡片槽位、商品网格及其父容器调用 `revalidate()`/`repaint()`。
- `ShopHomePanel` 在数据加载完成后通过下一次 EDT 调度恢复路由中保存的滚动位置。
- 自动化测试验证了窄宽度下网格首选高度包含多行，并验证翻页及从详情返回时保存/恢复滚动值。

**此前方案未能发现问题的原因：**

- `ProductGridPanelTest` 只在脱离真实窗口的面板上手动设置尺寸，断言首选高度“大于 500”，没有断言精确行数、精确内容高度、首行位置或多余空白。
- 测试没有把首页放入真实可见的 `JScrollPane` 和应用外壳后执行首次加载，因此没有覆盖首次布局、异步刷新、视口尺寸和滚动模型之间的组合行为。
- 分页测试直接操纵滚动条模型来验证状态保存，未验证初次进入时滚动值最终必须保持为 `0`。
- 自动化验收结论把局部布局计算通过误写成了问题已基本解决，但文档当时仍注明“待人工复测首屏高度”；本次截图证明该人工验收没有通过。

**当前根因定位：** 已确认是第一页首次布局时商品网格首选高度被过度计算并被外层滚动内容采用，而不是滚动位置恢复错误，也不是服务端返回了更多商品。当前 `WrappingGridLayout` 在网格尚未获得真实宽度（`parent.getWidth() == 0`）时，把可用宽度回退为单张卡片宽度，因此首次首选尺寸计算会暂时按“单列”计算全部商品行数；真实窗口随后可以按多列摆放卡片，但外层 `BoxLayout`/`JScrollPane` 已采用过大的首次高度。翻到第二页时网格已有真实宽度，首选高度重新按多列计算，所以恢复正常。新增回归测试已经复现并锁定这一计算错误。

**自动化整改证据（2026-09-01）：** 新增首次布局回归测试，在父容器已有 `900px` 宽、商品网格自身宽度仍为 `0` 时加载 20 个固定高度商品。修改前测试实际得到单列高度 `3828`，与问题现象一致；预期 4 列、5 行高度为 `948`。最小实现改为在网格尚无宽度时使用首个已有真实宽度的父容器计算列数。相关目录测试 23 项通过，完整 Maven 回归 Common 19、Server 191、Client 172，共 382 项，0 失败、0 错误、0 跳过。当前结论为“自动化已修复，待最终便携 Demo 人工复测”，尚不能仅凭自动化标记为已解决。

#### SHOP-TEST-003.1：商品卡底部按钮重复展示商品信息

**影响范围：** 所有同时展示多个商品的页面。
**严重程度：** 中高。

**现象：**

- 商品卡已经展示商品名、价格等信息。
- 卡片底部又显示包含商品名和价格的按钮，信息重复。
- 商品名称较长时，按钮文字被截断，增加视觉拥挤和理解成本。

**用户期望：**

- 移除卡片底部重复展示商品信息的购买按钮。
- 点击商品图片、名称或描述组成的商品区域即可进入商品详情。
- 该交互应覆盖首页、搜索结果、分类结果、店铺商品和其他多商品展示页面。

**验收关注点：**

- 商品信息不再重复展示。
- 整个商品卡片具有明确、统一的可点击反馈。
- 鼠标与键盘操作均可进入商品详情。
- 调整后不影响分页、滚动和商品网格排列。

#### SHOP-TEST-003.2：首页与搜索结果的店铺信息展示策略

**影响范围：** 商城首页和商品搜索结果。
**严重程度：** 中。

**用户期望：**

- 首页同时展示多个商品时，不显示店铺名称，降低首页信息密度。
- 用户执行搜索后，搜索结果应显示店铺名称，帮助区分商品来源。
- 通过首页分类入口进入的结果属于搜索结果，应按搜索页面规则显示店铺名称。

**验收关注点：**

- 首页商品卡不显示店铺名称。
- 搜索结果稳定显示店铺名称。
- 两类页面可以复用基础商品卡，但展示规则必须能够按使用场景切换。

### 3.2 搜索与筛选

#### SHOP-TEST-006：“筛选”与“应用筛选”语义混淆

**影响范围：** 从首页搜索后进入的商品搜索页面。
**严重程度：** 中。

**现象：**

- 搜索栏附近显示“筛选”按钮。
- 展开筛选区域后又显示“应用筛选”按钮。
- 两个按钮名称相近，但一个用于展开或收起区域，另一个用于执行筛选，容易被理解为重复功能。

**用户期望：**

- 展开或收起筛选区域与执行筛选应有清晰、不同的文字和视觉层级。
- 用户无需猜测两个按钮的差异。

**方案阶段需要确认：**

- 筛选条件是修改后自动应用，还是保留单独确认操作。
- 分类、价格和排序是否采用一致的刷新规则。
- 展开按钮与执行按钮最终采用的文字。

### 3.3 购物车与确认订单

#### SHOP-TEST-007.1：缺少商品小计与订单总价

**影响范围：** 购物车和确认订单页面。
**严重程度：** 严重。

**现象：**

- 购物车只能看到商品单价和数量，看不到单项小计与购物车总价。
- 确认订单页面只能看到商品及单价，看不到数量、小计和最终订单总价。
- 用户在提交订单前无法清楚确认最终支付金额。

**期望结果：**

每件商品至少展示：

- 商品名称与 SKU。
- 单价。
- 数量。
- 小计，即单价乘以数量。

页面汇总区域至少展示：

- 商品总数量。
- 订单总价。
- 进入结算或提交订单操作。

**验收关注点：**

- 修改数量后，单项小计、商品总数量和总价立即同步。
- 删除商品后，汇总金额立即同步。
- 确认订单金额与购物车及服务端计算结果一致。
- 最终金额在“提交订单”附近清晰可见。

#### SHOP-TEST-007.2：商品不应横向连续排列

**影响范围：** 购物车和确认订单页面。
**严重程度：** 高。

**现象：**

- 多件商品横向排列在同一行。
- 商品名称、价格、数量输入和按钮相互挤压。
- 商品较多或名称较长时难以阅读和操作。

**用户期望：**

- 商品采用纵向列表排列。
- 每件商品独占一行或一张横向信息卡。
- 多件商品从上到下排列，内容超过页面时使用纵向滚动。
- 购物车和确认订单保持一致的信息层级。

#### SHOP-TEST-007.3：购物车商品区域应进入商品详情

**关联问题：** `SHOP-TEST-003.1`。
**影响范围：** 购物车商品列表。
**严重程度：** 中。

**用户期望：**

- 点击购物车中的商品图片、商品名称或描述区域，进入对应商品详情。
- 数量输入框、“更新”和“删除”等操作保留自身行为，不触发详情跳转。
- 从商品详情返回后恢复购物车页面及原有滚动位置。
- 查看详情不能导致购物车数量或其他状态丢失。

### 3.4 开店申请

#### SHOP-TEST-002A：完整填写后不能直接提交审核

**影响角色：** 申请开店的学生和教师。
**严重程度：** 高。

**现象：**

- 用户完整填写申请内容并点击“提交审核”时，系统要求先保存草稿。
- 用户必须额外执行一次“保存草稿”，才能提交审核。

**用户期望：**

- 页面当前填写内容通过校验后，可以直接提交审核。
- 提交操作使用页面中的最新内容，不强制依赖已持久化的草稿。
- “保存草稿”继续作为暂时不提交时的独立功能。
- 被驳回申请修改完整后也应允许直接重新提交。

**验收关注点：**

- 新申请和被驳回后重提均支持直接提交。
- 校验失败时保留当前内容，并明确提示有问题的字段。
- 成功提交后状态正确变为待审核。

#### SHOP-TEST-002B：未保存内容离开时没有确认提示

**影响范围：** 开店申请页面的返回及商城工具栏导航。
**严重程度：** 高。

**现象：**

- 用户填写或修改申请内容后，如果没有保存草稿，离开申请区域时没有明确的数据保护提示。

**用户期望：**

当存在未保存修改并准备离开时，应提示用户选择：

- 保存草稿并离开。
- 不保存并离开。
- 取消离开并继续填写。

**验收关注点：**

- 返回、“我的”“购物车”及其他会离开申请页面的入口均遵守同一规则。
- 已保存且之后没有修改时不弹出提示。
- 选择取消后，所有页面内容保持不变。
- 选择保存后，草稿可在再次进入时恢复。

### 3.5 管理员审核

#### SHOP-TEST-004：审核列表未区分未处理与已处理

**影响角色：** 管理员。
**严重程度：** 中高。

**现象：**

- `PENDING`、`APPROVED` 和 `REJECTED` 申请混在同一列表中。
- 管理员无法快速区分需要操作的申请与历史记录。
- 当前列表没有清晰展示排序所依据的时间。

**用户期望：**

- 未处理区域只展示 `PENDING` 申请。
- 已处理区域展示 `APPROVED` 和 `REJECTED` 申请。
- 未处理和已处理都要按照时间排序。
- 管理员打开审核界面后能够优先定位未处理申请。

**验收关注点：**

- 处理完成的申请立即从未处理区域移动到已处理区域。
- 未处理列表展示提交时间，已处理列表展示审核时间。
- 时间相同时使用稳定的次级排序，刷新后顺序不跳动。
- 已处理记录保留审核状态、审核意见和时间。

**方案阶段需要确认：** 时间采用升序还是降序展示。

### 3.6 卖家商品与 SKU 管理

#### SHOP-TEST-005：卖家工作区暴露内部技术字段

**影响角色：** 获批店长；管理员商品管理界面存在相似表现。
**严重程度：** 高。

**现象：**

- 新增 SKU 时要求店长手工填写 `SKU ID`。
- 店长需要理解 ID 格式并自行保证唯一性，显著增加操作难度和出错概率。
- 卖家工作区直接展示较多数据库或接口层字段，整体操作方式偏技术化。

**用户明确要求：**

- SKU ID 不应由店长填写，应由系统自动生成。
- 卖家工作区界面需要整体整改。
- 本阶段先记录问题，后续根据全部问题统一策划方案。

**后续策划范围：**

- 区分商品和 SKU 的业务字段与内部技术字段。
- 梳理店长创建商品、增加 SKU、修改库存和上下架的最短操作路径。
- 评估 `SKU ID`、版本号、布尔状态和封面 URL 的展示方式。
- 明确管理员与店长商品编辑界面的复用边界。
- 图片继续使用占位策略，不升级数据库。

### 3.7 导航与返回逻辑

#### SHOP-TEST-008：我的与购物车污染返回历史

**影响范围：** 商城首页、我的、购物车之间的导航。
**严重程度：** 高。

**现象：**

- 进入首页后反复点击“我的”和“购物车”，每次切换都会继续追加返回历史。
- 点击“返回”时，会在“我的”和“购物车”之间不断切换。
- 用户需要多次返回，并且最终可能无法回到首页。
- 重复点击当前页面也可能增加无意义的历史记录。

**用户期望的导航关系：**

- “我的”和“购物车”属于同一层级的商城工具页。
- 两者之间切换时不应互相叠加返回历史。
- 从首页首次进入工具页时记住首页为来源。
- 在工具页之间切换时只替换当前工具页。
- 无论切换多少次，点击一次“返回”都回到首页。
- 重复点击当前工具页不产生新历史。

**必须通过的场景：**

1. 首页 → 我的 → 返回 → 首页。
2. 首页 → 购物车 → 返回 → 首页。
3. 首页 → 我的 → 购物车 → 返回 → 首页。
4. 首页 → 购物车 → 我的 → 返回 → 首页。
5. 首页 → 我的 → 购物车 → 我的 → 返回 → 首页。
6. 首页 → 购物车 → 我的 → 我的 → 返回 → 首页。

**后续策划范围：**

- 区分首页、搜索、店铺、详情等内容导航历史。
- 区分“我的”“购物车”等同级工具页切换。
- 单独定义结算、支付完成等流程页面的返回规则。

### SHOP-TEST-009：默认端口被旧服务端占用时，Demo 启动失败且诊断信息不足

**影响范围：** 最终便携 Demo 的服务端启动与人工验收准备。
**严重程度：** 高。

**复现步骤：**

1. 本机已有一个 Java 服务端监听 TCP `8888`。
2. 双击最终发布包中的 `启动服务端.bat`。
3. 启动脚本打印端口 `8888` 后，服务端抛出 `java.net.BindException: Address already in use: bind`。
4. 脚本仅提示检查端口和日志，随后进入暂停或终止批处理提示。

**本次测试证据：**

- Windows `netstat -ano` 显示 PID `37480` 同时监听 `0.0.0.0:8888` 和 `[::]:8888`。
- PID `37480` 为仍在运行的 `java.exe`，启动时间为 `2026-08-31 12:09:50`。
- `8888` 不在 Windows TCP 排除端口范围内。
- 当前 `启动服务端.bat` 只执行一次 `ShopAuthDemoServerMain`，不存在脚本自身重复拉起服务端的情况。
- 因此，本次失败的直接原因是旧 Java 服务端占用统一端口，不是新发布包内服务端在启动过程中自行重复绑定。

**体验问题：**

- 当前报错不能直接告诉测试者“已有服务端正在运行”以及占用进程信息。
- 测试者无法判断现有监听者是可直接复用的 vCampus 服务端、此前遗留的 Shop Demo，还是其他程序。
- 直接要求结束 Java 进程存在误停其他模块服务端的风险；在未确认进程身份前不应自动终止。
- 端口冲突会完全阻断最终 Demo 的后续人工验收。

**期望与后续策划范围：**

- 启动前检查 `8888` 是否已经处于监听状态。
- 端口被占用时给出明确、可操作的中文提示，至少说明端口、占用 PID 和建议的确认步骤。
- 区分“已有兼容的 vCampus 服务端可复用”和“其他进程冲突”两种情况，不能默认结束占用进程。
- 保留显式端口覆盖能力，用于隔离测试；最终联合验收仍以统一端口 `8888` 为基线。
- 增加启动脚本的端口占用自动化测试，并人工验证冲突提示在 Windows 终端中清晰可读。

**人工复测结果（2026-09-01）：** 用户确认该问题已经解决，端口冲突不再阻塞本轮 Demo 测试。上述启动诊断增强可作为后续体验改进，不再作为当前发布验收阻塞项。

### SHOP-TEST-010：卖家商品管理页缩小后左侧商品列表被严重压缩

**影响范围：** 店主开店后的卖家工作区 → 商品管理。
**严重程度：** 高。

**对比场景：**

1. 全屏显示卖家商品管理页时，左侧商品列表具有足够宽度，商品名、状态、SKU 数、最低价、总库存、预留库存和销量均能清楚显示。
2. 将主窗口缩小到普通非全屏尺寸。
3. 右侧商品编辑区仍占据大部分横向空间，左侧商品列表被压缩为很窄的一列。

**现象：**

- 左侧七列表格的表头大量显示为省略号或被截断。
- 商品名称、状态等数据也被缩略，用户无法直接辨认每一列的含义和商品信息。
- 左侧列表与右侧编辑区的宽度分配在缩小窗口后明显失衡。
- 页面仍能操作，但商品选择和信息对照的可读性、观感与使用效率显著下降。

**用户反馈：** 全屏状态显示比较清楚；缩小后左侧部分被严重压缩，明显影响观感。

**用户提出的根因判断：** 当前页面试图在同一个界面内同时容纳商品列表、商品资料、封面选择、规格列表和新增操作，导致左右两侧长期争抢空间；这种“一个界面完成全部功能”的设计思路不适合普通窗口尺寸。

**用户提出的改造方向：**

- 卖家商品管理主界面聚焦商品列表、商品选择和必要的概要查看。
- “创建商品”等新增内容操作改为独立弹窗，填写完成后提交或取消，再刷新主列表。
- “添加规格”同属新增内容操作，应优先采用独立弹窗，避免在主界面常驻完整新增表单。
- 弹窗应具有明确的确认和取消出口，取消时不能污染当前列表选择或已保存数据。
- 用户后续确认“更新现有商品”也改为独立弹窗，使主界面不再保留常驻编辑器。

**待后续设计确认：**

- 定义卖家商品管理页支持的最小可用宽度。
- 在新增操作移入弹窗后，重新确定主界面的信息层级与列表可用宽度。
- 确定剩余主界面区域在窗口缩小时的最小宽度、比例和收缩优先级。
- 评估在较窄窗口下采用横向滚动、隐藏次要列或其他响应式布局方式。
- 保证商品列表的关键列在目标窗口尺寸下仍可辨认，不以连续省略号作为正常展示状态。

**验收关注点：**

- 在全屏与普通非全屏窗口尺寸下分别截图验收。
- 缩小窗口后，商品列表不能被压缩为无法阅读的窄条。
- 调整窗口尺寸后布局应即时、稳定地重新分配空间，不要求用户切换标签页才能恢复。

**整改与人工验收结果（2026-09-02）：** 修改前新增布局测试直接发现卖家商品管理页仍包含横向 `JSplitPane` 和常驻商品编辑器。整改后主界面只保留全宽商品表格及创建、更新、上下架操作；创建与更新均通过可替换的商品编辑弹窗边界完成，规格仍通过独立规格弹窗编辑。`ProductManagementPanelTest` 共 6 项通过，并验证确认创建/更新后发送对应业务命令、SKU 身份仍在更新命令中保留。完整 Maven 回归 Common 19、Server 191、Client 175，共 385 项，0 失败、0 错误、0 跳过。用户在全屏和缩小窗口下复测后明确表示对 `SHOP-TEST-010` 的修改满意，该问题通过人工验收。

### SHOP-TEST-011：长浏览链受串行历史长度限制，最终无法返回首页

**影响范围：** 首页、商品详情、店铺商品、购物车之间的连续浏览与返回。
**严重程度：** 高。

**现象：**

- 当前普通浏览路径仍按访问顺序串行追加历史节点。
- 当用户在商品、购物车、店铺和其他商品之间连续跳转多次后，点击“返回”只能沿仍被保留的串行节点逐步回退。
- 串行历史具有长度上限；较早的首页节点被丢弃后，无论继续点击多少次返回，最终都无法回到商城首页。
- 此问题与此前 `SHOP-TEST-008` 不同：`SHOP-TEST-008` 只覆盖“我的”和“购物车”两个同级工具页互切，本问题覆盖普通内容浏览形成的长链。

**用户提供的复现路径：**

1. 从首页点击任意商品。
2. 将商品加入购物车并点击查看购物车。
3. 在购物车点击商品，再进入该商品所属店铺。
4. 在店铺点击另一件商品，加入购物车，再进入购物车。
5. 从购物车继续进入商品、店铺和其他商品详情。
6. 按上述模式重复约 5 至 10 次，使商品、店铺和购物车不断交替跳转。
7. 连续点击“返回”。
8. 实际结果：只能退回有限长度的浏览路径，最终无法回到首页。

**原实现关联：** `ShopNavigator` 使用串行 `Deque<ShopRoute>` 保存普通浏览历史，并设置 `MAX_HISTORY = 20`。历史超过上限时移除最早节点，因此长链中的首页路由会被永久丢弃。此前为工具页增加内容锚点并未改变普通商品/店铺浏览仍使用有限串行历史的根本结构。

**整改进展（2026-09-02）：** 用户已确认 [`2026-09-02-vcampus-shop-layered-navigation-design.md`](2026-09-02-vcampus-shop-layered-navigation-design.md)，并已按该设计完成固定槽位的内容路径、工具覆盖路径、支付完成态，以及侧栏/工具栏共用的强制回首页动作。

**用户提出的修改方向：**

- 修改整个商城浏览与返回的根本逻辑，不再把所有访问事件组成一条有限串行路径。
- 将导航历史改为分层结构。
- 进入下一类浏览内容时进入下一层。
- 同一类型的访问事件在当前层内移动或替换，不继续无上限地增加返回深度。
- “返回”只返回上一层，使首页始终作为稳定根层保留。
- 增加固定首页入口：无论当前位于商品、店铺、购物车、我的、结算或支付流程，点击应用左侧菜单栏的“校园商城”都强制重置商城导航并返回商城首页。
- 在商城顶部工具栏的“我的”左侧固定增加“返回首页”按钮，功能与点击左侧“校园商城”完全相同，并复用同一重置逻辑。
- 该强制入口应清除当前浏览层、工具覆盖层和未完成的纯客户端导航流程，避免再次进入商城时恢复到刚才的深层页面。

**已确认并实现的规则：**

- 明确定义首页、搜索结果、店铺、商品详情、购物车、结算、我的和卖家/管理员工作区分别属于哪一层。
- 明确“商品详情 → 店铺 → 另一商品详情”中哪些跳转进入下一层，哪些只替换当前层。
- 明确购物车作为全局工具页还是浏览层，以及从购物车点击商品后的返回目标。
- 明确结算与支付流程是否使用独立流程栈，避免破坏浏览层。
- 明确历史是否需要保存每层的查询、页码、筛选条件和滚动位置。

**验收关注点：**

- 按用户给出的路径循环 5 次、10 次后，连续返回仍能到达首页。
- 同类商品或店铺之间连续访问不能无限增加返回深度。
- 首页根层不能因访问次数或任何固定历史长度上限被丢弃。
- 分层整改不能破坏 `SHOP-TEST-008` 已定义的“我的/购物车互切后一次返回内容锚点”行为。
- 商品页、店铺页、购物车中已保存的页码、筛选和滚动位置应按最终分层规则正确恢复。
- 在任意商城页面点击左侧“校园商城”，都立即回到商城首页第 1 页顶部，并重置返回状态；随后点击商城内“返回”不能再次回到重置前的深层页面。
- 在任意商城页面点击工具栏“返回首页”也满足同一验收结果；该按钮固定显示在“我的”左侧。

**自动化整改证据（2026-09-02）：**

- `ShopNavigationStateTest.userReportedCycleIsBoundedAndAlwaysReturnsHome` 参数化执行 5、10、100 次用户长链循环；每种场景的语义节点数都不超过 4，最多 4 次返回即可到达首页。
- `ShopNavigationStateTest.repeatedStoreAndProductBrowsingNeverGrowsTheState` 与 `ShopNavigatorTest.repeatedProductsReplaceOneDetailLayerAndKeepHomeReachable` 验证同层店铺/商品访问只替换固定槽位。
- `PurchasePanelsTest.cartProductPreviewReturnsToCartAndCheckoutPreviewReturnsToCheckout` 验证购物车和确认订单的商品预览分别返回原页面；支付完成测试验证所有可返回目标均不包含 `Checkout`。
- `ShopUiTest.goHomeResetsEveryLayerToFirstPageTopAndCannotReturn`、`originalShopEntryCallsGoHomeExactlyOncePerClick` 与 `toolbarAndCoordinatorHomeActionsShareTheSellerApplicationLeaveGuard` 验证工具栏和侧栏使用同一个默认首页重置动作，并共同遵守未保存申请离开保护。
- 聚焦导航检查共 66 项通过；最新完整 `mvn -pl vcampus-client -am test` 与 `mvn verify` 均运行 Common 19、Server 191、Client 188，合计 398 项，0 失败、0 错误、0 跳过。Demo 启动脚本与便携包脚本测试均通过，默认端口为 8888。
- 当前结论：自动化已整改，待人工复测。

### SHOP-TEST-012：开店申请缺少字数限制，审核详情无法承载长文本

**影响范围：** 学生或教师填写开店申请，以及管理员查看和审核开店申请。
**严重程度：** 高。

**现象：**

- 店铺名称、联系方式和经营计划目前缺少明确的输入字数限制。
- 管理员审核界面将联系方式和经营计划直接拼接成表格下方的一行文本。
- 当经营计划输入超长内容时，文字会在页面底部持续横向延伸并被截断，管理员无法完整阅读申请内容。
- 当前展示方式没有独立阅读区域、换行和滚动能力，不适合联系方式和经营计划这类长度不同的字段。

**用户提供的复现步骤：**

1. 使用申请人账号进入开店申请页面。
2. 在经营计划中输入超长文本并提交审核。
3. 使用管理员账号进入“商城管理 → 开店审核 → 未处理”。
4. 选中该申请。
5. 实际结果：联系方式和经营计划作为一行文本显示在表格下方；经营计划被截断，无法完整查看。

**期望结果与修改建议：**

- 为店铺名称、联系方式和经营计划分别设置合理且明确的字数上限。
- 申请人输入超过上限时应立即获得清楚的提示，提交和保存草稿也必须执行相同校验。
- 管理员审核主界面不再把联系方式和经营计划直接复制到表格下方。
- 管理员点击表格中对应的申请记录时，打开申请详情弹窗。
- 弹窗完整展示联系方式和经营计划；经营计划应自动换行，并在内容较长时提供纵向滚动。
- 通过和驳回操作仍针对当前选中的申请，不能因打开或关闭详情弹窗而改变选择。

**待后续设计确认：**

- 店铺名称、联系方式和经营计划各自的具体字数上限。
- 单击整行、双击整行或点击“查看详情”按钮中的最终触发方式；用户当前建议为点击表格中的申请人信息后弹窗查看。
- 详情弹窗是否只读，以及“通过”“驳回”操作保留在主界面还是移入弹窗。

**当前状态（2026-09-02）：** 已记录用户截图和复现路径，尚未设计或实施整改。

**自动化整改证据（2026-09-02）：** 已按用户确认规则将店铺名称、联系方式、经营计划上限分别设为 50、50、500 字；申请输入区提供灰色上限提示和右上角剩余字数，粘贴超长文本也不能超过边界。服务端保存草稿入口执行同一长度校验。审核页单击行仅选择，双击未处理或已处理记录打开只读详情；未处理记录可在主界面或弹窗审核，已处理记录的审核操作禁用。`SellerApplicationServiceTest`、`SellerApplicationPanelTest`、`ApplicationReviewPanelTest` 聚焦测试通过；当前结论为“自动化已整改，待最终 Demo 人工复测”。

### SHOP-TEST-013：店主创建商品弹窗无法调整大小

**影响范围：** 店主工作区的“创建商品”操作及其商品、封面和规格编辑区域。
**严重程度：** 高。

**现象：**

- “创建商品”采用独立弹窗后，弹窗尺寸固定，用户无法拖动窗口边缘或角落调整大小。
- 当前固定尺寸下，表单和规格表格没有根据可用空间合理布局，内容区域同时出现横向和纵向滚动条。
- 用户无法通过放大弹窗改善字段、封面选项和规格表格的可读性，也无法根据屏幕大小缩放到合适尺寸。

**用户提供的复现步骤：**

1. 使用已通过开店申请的店主账号进入“卖家工作区 → 商品管理”。
2. 点击“创建商品”。
3. 尝试拖动弹窗边缘或角落改变窗口大小。
4. 实际结果：弹窗无法拉伸，表单内部保留大面积空白并出现横向、纵向滚动条。

**期望结果：**

- 创建商品弹窗应允许用户自由调整大小。
- 弹窗应设置合理的初始尺寸和最小尺寸，首次打开时完整显示主要字段、规格列表以及“确定”“取消”操作。
- 用户放大或缩小弹窗时，输入区、封面选项和规格表格应随可用空间稳定伸缩。
- 在达到最小尺寸之前不应出现横向滚动条；纵向滚动只用于确实超出可视高度的表单内容。
- 底部“确定”“取消”操作应保持可见，不应因滚动表单而丢失。

**待后续设计确认：**

- 弹窗的默认宽高、最小宽高及是否记忆用户上次调整后的尺寸。
- 规格列表在窗口高度增加时是否优先获得额外空间。
- 创建商品与更新商品弹窗是否共用同一套可伸缩窗口规范。

**当前状态（2026-09-02）：** 已记录用户截图和复现路径，尚未设计或实施整改。

**自动化整改证据（2026-09-02）：** 创建商品与修改商品信息共用可调整大小的模态窗口，初始尺寸为 `900×650`，最小尺寸为 `720×520`，每次相对父窗口居中且不记忆上次尺寸；商品编辑区随窗口扩展，确定和取消操作固定在底部，横向滚动条禁用。`SwingProductEditorDialogsTest` 与 `ProductManagementPanelTest` 聚焦测试通过；真实窗口拖拽与最小尺寸视觉效果仍待最终 Demo 人工复测。

### SHOP-TEST-014：卖家工作区存在技术术语和不准确操作文案

#### SHOP-TEST-014.1：用户界面的“SKU”应统一改为“商品种类”

**影响范围：** 卖家工作区以及整个软件中所有面向用户展示的表头、标签、按钮、提示、错误信息和说明文字。
**严重程度：** 中高。

**现象：**

- 卖家商品管理列表仍显示“SKU 数”列。
- “SKU”属于内部技术术语，普通店主难以直接理解其业务含义。
- 此前虽然隐藏了 SKU ID 等内部字段，但用户可见文案仍未完成全局业务化整改。

**期望结果：**

- 检查整个软件中所有面向用户显示的“SKU”文字，并统一改为“商品种类”。
- 卖家商品管理列表的“SKU 数”改为“商品种类数”。
- 创建商品、修改商品、管理员商品管理、商品详情、购物车、订单及提示信息中的相关文字采用同一术语。
- 本次要求针对用户可见文案；内部代码标识、数据库字段和网络契约是否重命名需在实施前单独评估，不能因文案整改破坏兼容性。

**验收关注点：**

- 对客户端所有 Shop 页面执行一次用户可见文案扫描。
- 人工遍历买家、店主和管理员界面，不能再看到面向用户展示的“SKU”字样。
- 同一业务概念不得在不同页面混用“SKU”“规格”“商品种类”等多套名称。

#### SHOP-TEST-014.2：“更新所选商品”应改为“修改商品信息”

**影响范围：** 卖家工作区商品管理页。
**严重程度：** 中。

**现象：** 页面底部按钮使用“更新所选商品”，偏向系统数据操作表述，不能清楚表达店主将进入商品信息编辑流程。

**期望结果：**

- 将按钮文字“更新所选商品”改为“修改商品信息”。
- 按钮行为保持为打开当前选中商品的修改弹窗。
- 未选中商品时的提示文案也应使用“修改商品信息”的业务表述。

**当前状态（2026-09-02）：** 两个文案问题均已根据用户截图记录，尚未实施整改。

**自动化整改证据（2026-09-02）：** client/shop 用户可见文案已将 `SKU` 和同义“规格”统一为“商品种类”，包括商品种类数、商品种类名称、添加/编辑商品种类及订单详情；内部 `Sku` 类名、`skuId`、数据库字段、资源锁键和网络契约保持不变。“更新所选商品”已统一改为“修改商品信息”。源码用户可见字符串扫描无 `SKU`、“规格”或旧按钮文案残留，相关 UI 聚焦测试通过；待四角色界面人工遍历确认。

### SHOP-TEST-015：管理员商品管理页缩小后左侧表格被严重压缩

**影响范围：** 管理员“商城管理 → 商品管理”页面中的店铺列表、商品列表及商品维护区域。
**严重程度：** 高。

**对比场景与现象：**

1. 全屏显示管理员商品管理页时，左侧店铺表格能够辨认店铺 ID、店主、店铺名称、类别和状态，下方商品表格也具有可用宽度。
2. 将主窗口缩小到普通非全屏尺寸。
3. 右侧常驻商品资料和规格编辑区域继续占据大部分横向空间。
4. 实际结果：左侧店铺表格和商品表格被压缩为窄条，多个表头与内容只显示省略号，管理员无法辨认和选择目标店铺或商品。

**与既有问题的关系：**

- 此问题与 `SHOP-TEST-010` 的根因和表现相同，但发生在管理员商品管理页面。
- 用户已确认 `SHOP-TEST-010` 的卖家端整改效果满意，并明确建议管理员端参考相同方案。
- `SHOP-TEST-010` 已通过将主页面恢复为全宽列表、把创建和修改功能移入独立弹窗解决左右区域争抢空间的问题。

**用户确认的参考整改方向：**

- 管理员商品管理主界面不再常驻右侧完整商品编辑器和规格编辑器。
- 主界面优先保证店铺列表、所选店铺的商品列表及必要概要信息可读。
- “为所选店铺创建商品”和“修改商品信息”使用独立弹窗，确认后刷新列表，取消时不修改数据。
- 规格的新增和修改继续使用独立弹窗，不在主界面长期占据横向空间。
- 普通非全屏尺寸下，关键表头和数据不能普遍显示为省略号。

**验收关注点：**

- 分别在全屏和用户截图所示的普通窗口尺寸下验证管理员商品管理页。
- 店铺列表和商品列表均应获得足以辨认关键列的宽度。
- 调整窗口尺寸时布局应即时稳定，不依赖切换标签页重新计算。
- 创建商品、修改商品、添加或修改商品种类的弹窗操作不得破坏当前店铺和商品选择。
- 管理员端整改后的信息结构和交互方式应与已通过人工验收的卖家端方案保持一致。

**当前状态（2026-09-02）：** 已记录两张全屏/缩小对比截图，并确定参考 `SHOP-TEST-010` 的已验收方案；尚未实施管理员端整改。

**自动化整改证据（2026-09-02）：** 管理员商品管理主界面已移除横向分割和常驻商品编辑器，店铺表与商品表在全宽区域纵向排列；创建和修改商品信息复用独立可调整弹窗。异步详情继续使用请求代次保护，提交命令保留明确 `shopId`，刷新后按稳定 `productId` 恢复商品选择，取消弹窗不发送命令。`AdminProductManagementPanelTest` 4 项通过；普通非全屏尺寸下的实际列宽和缩放稳定性待最终 Demo 人工复测。

### SHOP-TEST-016：商品详情页“店铺”按钮应改为“进入店铺”

**影响范围：** 买家商品详情页。
**严重程度：** 低。

**现象：** 商品详情页使用“店铺”作为按钮文字，只描述了目标对象，没有明确表达点击后将进入该商品所属店铺。

**期望结果：**

- 将商品详情页按钮文字“店铺”改为“进入店铺”。
- 按钮原有的店铺跳转行为保持不变。
- 相关自动化测试和无障碍名称同步采用“进入店铺”。

**当前状态（2026-09-02）：** 已根据用户截图记录，尚未实施整改。

**自动化整改证据（2026-09-02）：** 商品详情按钮已改为“进入店铺”，原有店铺路由行为保持不变；`CatalogPanelsTest.detailStoreButtonOpensTheLoadedStorefront` 同时验证新文案和目标店铺跳转。待最终 Demo 人工确认按钮展示与操作感受。

### SHOP-TEST-017：购物车缺少移除商品功能

**影响范围：** 买家购物车页面。
**严重程度：** 高。

**现象：**

- 购物车中的商品行没有面向用户的“移除”操作。
- 用户将商品加入购物车后，无法删除已经不准备购买的商品。

**期望结果：**

- 每个购物车商品行提供明确的“移除”操作。
- 移除成功后，商品列表、购物车商品数量和金额汇总同步刷新。
- 移除操作只影响目标购物车项，不得影响其他商品。

**当前状态（2026-09-02）：** 自动化整改已完成；每行提供“移除”并在执行前确认，成功后使用服务端返回刷新列表、数量和金额。`PurchasePanelsTest` 覆盖取消不请求、确认只移除目标项。待人工验收。

### SHOP-TEST-018：购物车无法选择部分商品结算

**影响范围：** 买家购物车和确认订单流程。
**严重程度：** 高。

**现象：**

- 当前结算会提交购物车中的全部商品。
- 用户无法选择其中一部分商品支付，也无法将未选商品继续保留在购物车中。

**期望结果：**

- 每个购物车商品行提供选择框，并提供全选和取消全选操作。
- 进入确认订单和支付流程时只包含已选商品，金额汇总也只统计已选商品。
- 结算成功后仅移除已经结算的商品，未选商品继续保留在购物车中。
- 未选择任何商品时不能进入结算，并应显示明确提示。

**当前状态（2026-09-02）：** 自动化整改已完成；购物车默认全部不勾选，支持全选/取消全选，仅按选中 ID 进入确认与结算，选中项失效时阻止提交并提示重新选择。`PurchasePanelsTest` 与 `CheckoutServiceTest` 证明未选商品保留且角标重新同步。待人工验收。

### SHOP-TEST-019：开店申请与管理员审核页面缺少刷新按钮

**影响范围：** 店主开店申请页面，以及管理员开店申请审核页面。
**严重程度：** 中。

**现象：**

- 申请人无法主动获取申请的最新审核状态。
- 管理员无法主动获取新提交的申请或其他操作产生的最新审核结果。

**期望结果：**

- 开店申请页面和管理员审核页面均提供明确的“刷新”按钮。
- 刷新期间避免重复请求，并向用户反馈加载状态。
- 刷新完成后展示服务端最新数据，同时合理保留当前分页、分组和有效选择。
- 申请页面存在未保存输入时，刷新不能静默覆盖用户内容。

**当前状态（2026-09-02）：** 自动化整改已完成；申请主页面改为摘要与刷新，填写/修改使用模态弹窗，支持保存草稿、直接提交和关闭时保存/不保存/取消。管理员刷新一次并行重载未处理、已处理列表，两个请求完成前按钮保持禁用。相关 9 项聚焦测试通过，待人工验收。

### SHOP-TEST-020：店主商品管理未按上架状态分列管理

**影响范围：** 店主商品管理页面和商品状态流转。
**严重程度：** 高。

**现象：**

- 上架、下架和草稿商品混合显示在同一张商品表中。
- 店主不能直观区分当前在售商品和未上架商品，也难以确认状态切换后的归属。

**用户确认的期望布局与规则：**

- 商品管理主界面分为左右两列：左列为“上架商品”，右列为“下架商品”。
- “上架商品”列只显示 `ACTIVE` 商品。
- “下架商品”列同时显示 `INACTIVE` 和 `DRAFT` 商品；草稿不单独设置区域，状态显示为“草稿”。
- 商品上架或下架成功后自动移动到对应列表。
- 两列均可选中商品并执行当前状态允许的修改或状态操作，普通窗口尺寸下仍应保持可读。
- 草稿商品不能直接上架。店主完善信息后，必须先执行“完成商品编辑”，将状态从“草稿”转为“已下架”，之后才允许上架。

**待后续设计确认：**

- “完成商品编辑”所需的字段完整性和商品种类可售条件。
- 左右两列在最小窗口尺寸下的宽度、滚动和操作按钮布局。
- 状态切换失败时的选择保留、错误提示和列表恢复方式。

**当前状态（2026-09-02）：** 自动化整改已完成；左列仅显示已上架商品，右列显示已下架与草稿。草稿操作为“完成商品编辑”并迁移到 `INACTIVE`，服务端拒绝 `DRAFT -> ACTIVE`，之后才能上架。服务端 15 项、客户端 10 项聚焦回归通过，待人工验收。

### SHOP-TEST-021：用户可见报错码不完整且未统一为中文

**影响范围：** Shop 买家、店主和管理员界面的全部失败提示，包括商品、购物车、结算、支付、开店申请、审核、店铺状态和订单操作。
**严重程度：** 高。

**现象：**

- 部分失败场景缺少对应的用户提示映射，界面可能直接显示裸错误码、英文消息或底层技术异常。
- 同类错误在不同页面的表达不一致，用户难以理解失败原因和下一步处理方式。

**期望结果：**

- 盘点 Shop 全部服务端业务错误码以及客户端可能收到的网络、超时和未知异常。
- 为每个用户可见失败场景提供清晰、准确且一致的中文提示，不直接向用户暴露英文异常、堆栈、内部类名或数据库信息。
- 中文提示应说明发生了什么；存在可恢复路径时，还应说明用户可以采取的下一步操作。
- 相同错误码在不同页面保持核心含义一致，页面可根据操作上下文补充必要说明。
- 未识别错误使用统一的中文兜底提示，同时保留内部错误码用于日志和诊断。
- 内部错误码、数据库结构和公共网络契约保持稳定；本问题只整改用户可见映射及必要的 Shop 模块错误定义。

**验收关注点：**

- 建立服务端错误码与客户端中文提示的完整对应清单，并通过自动化测试防止遗漏。
- 人工遍历买家、店主和管理员的主要失败路径，界面不得出现裸错误码或英文技术消息。
- 日志仍应保留原始错误码和诊断信息，不能因中文化降低排障能力。

**当前状态（2026-09-02）：** 自动化整改已完成；`ShopUiErrors` 对全部 `ShopErrorCode`、认证/校验/内部/网络失败提供统一中文文案，未知错误使用中文兜底，原始错误码仍用于会话判断与日志诊断。`ShopUiErrorsTest` 31 项通过，待人工遍历代表性失败路径。

### SHOP-TEST-022：统一主题按钮首选宽度为 0，导致商城主要操作不可见

**影响范围：** Shop 买家、店主和管理员共用工具栏、筛选区、分页区及所有依赖组件首选尺寸进行布局的按钮区域。
**严重程度：** 高。

**人工复现环境（2026-09-03）：**

- 隔离工作树：`E:\summer-school\vCampus\.worktrees\shop-auth-demo`
- 分支：`integration/user-all-modules`
- HEAD：`d6e3651091b8e38dab49fd1ef5af3d0697076e89`
- 当前源码以 `mvn -DskipTests package` 重新打包后，从 `vcampus-distribution` 启动。
- 服务端成功监听 `8888`；客户端窗口约为 `1280×800`。
- 使用学生 `213230001` 和店主 `SHOPOWNER` 分别登录后进入“校园商城”，两者结果一致。

**现象：**

- 商城首页搜索输入框可见，但“搜索”按钮不可见。
- “文具、图书、生活用品、药品、其他”等分类按钮全部不可见。
- Shop 工具栏的“返回首页”“我的”“购物车”等操作不可见。
- 商品卡只剩局部边框和文字，内置封面区域表现为空白，页面整体出现大面积异常留白。
- 等待异步加载完成后界面没有恢复，因此不是加载中间态或截图延迟。
- 因主要导航入口不可用，购物车、结算、卖家工作区、管理员工作区和后续弹窗场景无法继续进行可靠的人工操作验收。

**定位证据：**

- `SharedShopUiKitAdapter.styleButton(...)` 对所有语义按钮执行 `button.setPreferredSize(new Dimension(0, 32))`。
- `FlowLayout` 和 Shop 工具栏等布局使用按钮首选宽度进行排版，宽度 `0` 会使按钮在真实 Swing 窗口中被压缩到不可见。
- 自动化测试仅验证了按钮首选高度和颜色，没有验证文本按钮的首选宽度为正数，因此 Task 1–8 的结构测试未捕获该真实布局回归。

**期望结果：**

- 统一按钮只约束高度，不把首选宽度固定为 `0`；首选宽度应能容纳中文文本、边距和边框。
- 在 `1280×800`、`1024×680` 与 Windows 150% 缩放下，工具栏、筛选、分页及对话框按钮均可见、可点击且文字完整。
- 增加真实布局回归测试：按钮加入 `FlowLayout`/工具栏并完成布局后，宽度必须大于 0，关键按钮文字不得被裁剪。

**当前状态（2026-09-03）：** 已按 TDD 整改。新增真实 `FlowLayout` 回归测试，修改前准确得到三个语义按钮宽度均为 `0`；整改后保留 Swing 按文字和边距计算的首选宽度，仅统一高度为 `32px`。`ShopUiKitTest` 13 项以及 Shop UI 聚焦回归 155 项通过（0 失败、0 错误，7 项既有环境跳过）。重新打包并使用学生 `213230001` 实机复测后，“搜索”、五个分类、“返回首页”“我的”“购物车（4）”及分页按钮均恢复显示。该阻断已解除，Task 9 其余页面与业务路径仍待继续人工验收。

## 4. 严重程度归类

### 严重

- `SHOP-TEST-007.1`：购物车和确认订单缺少小计与总价，影响用户确认实际支付金额。

### 高

- `SHOP-TEST-001`：首次进入商城页面高度异常。
- `SHOP-TEST-002A`：完整申请不能直接提交。
- `SHOP-TEST-002B`：未保存申请离开时缺少保护。
- `SHOP-TEST-005`：SKU ID 等内部字段增加店长操作难度。
- `SHOP-TEST-007.2`：购物车和确认订单横向排列，严重影响可读性。
- `SHOP-TEST-008`：工具页切换污染返回历史。
- `SHOP-TEST-009`：旧服务端占用统一端口时，Demo 无法启动且诊断信息不足。
- `SHOP-TEST-010`：卖家商品管理页缩小后左侧商品列表被严重压缩。
- `SHOP-TEST-011`：长浏览链受串行历史长度限制，最终无法返回首页。
- `SHOP-TEST-012`：申请字段缺少长度边界，超长经营计划导致管理员无法完成内容审核。
- `SHOP-TEST-013`：创建商品弹窗无法拉伸，固定布局与双向滚动严重影响店主操作。
- `SHOP-TEST-015`：管理员商品管理页缩小后店铺和商品表格被压缩到无法辨认。
- `SHOP-TEST-017`：购物车缺少移除商品能力，用户无法清理不再购买的商品。
- `SHOP-TEST-018`：购物车只能全量结算，无法选择部分商品支付并保留其余商品。
- `SHOP-TEST-020`：店主商品管理未按上架状态分列，且缺少明确的草稿完成流程。
- `SHOP-TEST-021`：用户可见错误映射不完整，裸错误码和英文技术消息影响理解与恢复操作。
- `SHOP-TEST-022`：统一主题按钮首选宽度为 0，商城主要导航和操作入口在真实窗口中不可见。

### 中高

- `SHOP-TEST-003.1`：商品卡重复按钮造成信息冗余。
- `SHOP-TEST-004`：管理员无法快速区分待处理与已处理申请。
- `SHOP-TEST-014.1`：用户界面仍暴露“SKU”技术术语，未统一为“商品种类”。

### 中

- `SHOP-TEST-003.2`：首页和搜索结果的店铺信息层级不合理。
- `SHOP-TEST-006`：筛选相关按钮语义混淆。
- `SHOP-TEST-007.3`：购物车商品区域不能进入商品详情。
- `SHOP-TEST-014.2`：“更新所选商品”没有准确表达修改商品信息的操作目的。
- `SHOP-TEST-019`：申请人和管理员审核页面缺少主动刷新入口。

### 低

- `SHOP-TEST-016`：商品详情页“店铺”按钮没有明确表达进入店铺的动作。

## 5. 后续策划顺序

以下顺序只用于安排后续设计讨论，不代表已经确定具体实现：

1. **优先保障金额与导航正确表达。** 先策划购物车、确认订单金额展示，以及“我的”“购物车”的返回模型。
2. **处理数据丢失风险。** 策划申请直接提交和未保存内容退出保护。
3. **整改关键业务工作区。** 统一策划卖家商品/SKU 操作与管理员审核信息架构。
4. **统一商品列表交互。** 处理商品卡点击区域、重复按钮、店铺字段和首次布局问题。
5. **简化搜索筛选。** 最后确定筛选区展开、条件应用和即时刷新规则。

在进入实施前，应针对每一类问题形成经过确认的交互方案和测试计划，再按优先级分批修改，避免把多个独立导航或页面状态问题混在同一批实现中。

## 6. 整改验收结果（2026-09-01）

本轮已完成自动化整改验收，原始观察保留在上文。自动化结果不能替代用户在真实窗口中的视觉与操作确认。

| 问题 | 自动化证据 | 当前结论 |
| --- | --- | --- |
| SHOP-TEST-001 | 新增首次零宽度布局回归：修改前 `3828`、预期并修改后 `948`；相关 23 项及最新完整 385 项回归通过 | 自动化已修复；待重新打包并人工复测第一页滚动范围与分页位置 |
| SHOP-TEST-002A/B | `SellerApplicationPanelTest`、`ShopNavigatorTest` | 直接提交、保存失败、保存/丢弃/取消离开路径通过 |
| SHOP-TEST-003.1/3.2 | `CatalogPanelsTest`、`ProductGridPanelTest` | 整卡激活、首页隐藏店铺、搜索显示店铺通过 |
| SHOP-TEST-004 | `ApplicationReviewPanelTest`、`AccessShopRepositoryTest` | 未处理/已处理分组及最新优先查询通过 |
| SHOP-TEST-005 | `ProductManagementPanelTest`、`AdminProductManagementPanelTest`、`CoverPresetPickerPanelTest` | SKU 技术字段隐藏、业务字段编辑、内置封面选择通过 |
| SHOP-TEST-006 | `CatalogPanelsTest`、`CatalogPaginationTest` | 搜索与应用筛选语义、排序即时刷新通过 |
| SHOP-TEST-007.1/2/3 | `PurchasePanelsTest`、`ShopUiTest` | 单价/数量/小计/权威总计、纵向确认清单、商品详情入口通过 |
| SHOP-TEST-008 | `ShopNavigatorTest` 六组参数化场景 | 工具页互切后一次返回内容锚点通过 |
| SHOP-TEST-009 | 本次人工启动与 `netstat -ano`、进程信息、端口排除范围检查；用户人工复测确认 | 端口冲突已解除，不再阻塞 Demo 测试；诊断提示增强降为可选改进 |
| SHOP-TEST-010 | 修改前测试发现横向分割；整改后 `ProductManagementPanelTest` 6 项及完整 385 项回归通过；用户完成全屏和缩小窗口复测 | 自动化与人工验收均通过；用户明确表示对整改满意 |
| SHOP-TEST-011 | 5/10/100 次参数化长链、固定节点上限、购物车/结算预览返回、支付终结、侧栏与工具栏强制回首页测试；聚焦 66 项及完整 398 项回归通过 | 自动化已整改，待人工复测 |
| SHOP-TEST-012 | 用户以超长经营计划人工复现，截图显示审核页底部单行文本被截断 | 新发现，已记录；待确认字段上限与详情弹窗交互后实施 |
| SHOP-TEST-013 | 用户人工打开创建商品弹窗并尝试调整窗口，截图显示固定尺寸及横向、纵向滚动条 | 新发现，已记录；待确认弹窗尺寸与伸缩布局规范后实施 |
| SHOP-TEST-014.1/14.2 | 用户截图显示“SKU 数”和“更新所选商品”；待执行全局用户文案扫描 | 新发现，已记录；待统一“商品种类”和“修改商品信息”文案 |
| SHOP-TEST-015 | 用户提供管理员商品管理页全屏/缩小对比截图；确认可参考已验收的 `SHOP-TEST-010` 方案 | 新发现，已记录；待将管理员端常驻编辑器迁移为弹窗并释放列表宽度 |
| SHOP-TEST-016 | 用户截图确认商品详情页按钮当前显示为“店铺” | 新发现，已记录；待改为“进入店铺” |
| SHOP-TEST-017 | `PurchasePanelsTest` 覆盖移除按钮、取消确认及单项移除 | 自动化已整改；待人工验收 |
| SHOP-TEST-018 | `PurchasePanelsTest`、`CheckoutServiceTest`、Shop 端到端测试覆盖选中范围与未选项保留 | 自动化已整改；待人工验收 |
| SHOP-TEST-019 | `SellerApplicationSummaryTest`、`SwingSellerApplicationDialogTest`、`ApplicationReviewPanelTest` | 模态申请、草稿保护及双方刷新已通过；待人工验收 |
| SHOP-TEST-020 | `ProductServiceTest`、`ProductManagementPanelTest` 及 Shop 端到端测试 | 双列布局与草稿受控迁移已通过；待人工验收 |
| SHOP-TEST-021 | `ShopUiErrorsTest` 穷举全部 Shop 错误码并覆盖公共/未知错误 | 自动化已整改；待人工遍历代表性失败路径 |

最新完整仓库验证：`mvn verify` 共运行 Common 19、Server 191、Client 188，合计 398 项，0 失败、0 错误、0 跳过。Shop Demo 启动脚本与便携包脚本测试均通过，默认端口为 8888。

本轮 SHOP-TEST-017 至 SHOP-TEST-021 整改后的最新完整验证：`mvn verify` 为 `BUILD SUCCESS`；Common 19、Server 195、Client 239，0 失败、0 错误。Client 中 7 个旧“主页面内嵌申请表”契约测试因表单迁移至模态弹窗而明确跳过，由新增的摘要页与弹窗测试替代。默认端口和既有 Demo 启动脚本未修改。

本次分层导航整改后的最新回归：`mvn -pl vcampus-client -am test` 共运行 Common 19、Server 191、Client 188，合计 398 项，0 失败、0 错误、0 跳过。

仍需人工执行：使用四个独立客户端窗口逐项复测上述界面表现，并记录截图。与 `origin/feat/user-management@8d53d9e` 的最终联合编译和登录/首次改密/账户页/退出联动，需要在获得明确集成授权并引入该提交后执行。
