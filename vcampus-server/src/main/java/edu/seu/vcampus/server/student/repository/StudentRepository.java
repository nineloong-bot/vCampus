package edu.seu.vcampus.server.student.repository;

/** Public compatibility facade for StudentRepository persistence. */
public final class StudentRepository extends StudentRepositoryOperations1 {
    /** Creates a stateless StudentRepository repository. */
    public StudentRepository() {
    }
}
