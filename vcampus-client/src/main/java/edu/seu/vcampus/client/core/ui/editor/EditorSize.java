package edu.seu.vcampus.client.core.ui.editor;

/** Describes how much page space an embedded editor needs. */
public enum EditorSize {
    /** A short form that may fit beside a list on a wide page. */
    COMPACT,
    /** A complex form that always opens below its list. */
    WIDE
}
