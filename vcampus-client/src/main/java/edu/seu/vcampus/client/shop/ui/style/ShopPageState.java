package edu.seu.vcampus.client.shop.ui.style;

/** Semantic states rendered by the Shop UI kit. */
public enum ShopPageState {
    INITIAL, LOADING, NORMAL, EMPTY, ERROR, DISCONNECTED, SUBMITTING, CONFLICT
}
